# Jazzy GPU dependency set: Whisper (CTranslate2) and Parakeet (NeMo) in one
# environment. Resolved versions are pinned in requirements.lock, which the
# Dockerfile passes as a constraint file; regenerate it with:
#   uv pip compile requirements_general.txt -o requirements.lock \
#       --extra-index-url https://download.pytorch.org/whl/cu121
#
# ONNX Runtime is the CPU build on purpose: openWakeWord requires the
# 'onnxruntime' distribution, and having onnxruntime-gpu instead makes NeMo's
# dependency validation fail. Torch/CTranslate2 still use the GPU.
#
# CPU builds use requirements_general_cpu.txt and do NOT include Parakeet.

numpy>=1.20.2,<2.0.0
sounddevice==0.5.5
librosa==0.11.0
numba==0.63.1
coverage==7.13.2
faster-whisper==1.2.1
openwakeword==0.4.0


torch>=2.0,<2.5
torchaudio>=2.0,<2.5
diart>=0.9.2
pyannote.core>=4.5
rx>=3.2
huggingface_hub==0.34.4
pymongo>=4.5
lark==1.3.1 # for hri_msgs
empy==4.2 # for hri_msgs
catkin_pkg
matplotlib==3.6.3

# Combined ASR environment (ONNX CPU, Torch/CTranslate2 GPU).
nemo_toolkit[asr]==2.4.0
transformers==4.51.3
onnxruntime==1.24.1
torch==2.4.1+cu121
torchaudio==2.4.1+cu121
torchvision==0.19.1+cu121
setuptools==75.8.2
